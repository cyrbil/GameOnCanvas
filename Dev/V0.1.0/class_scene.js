function scene(){
    
    // method call every frame, it's for multiples frames things like smooth move
    // it update position/size ... etc.
    scene.prototype.tick = function(){
        
    }
    
    // just draw on canvas
    scene.prototype.show = function(canvas){
        
    }
}